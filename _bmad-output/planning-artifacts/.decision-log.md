# PRD Decision Log

_Bootstrapped 2026-05-22 by reverse-engineering the existing PRD (no prior log existed). Entries below are inferred from PRD text, not from original discussions._

## Decisions (bootstrapped)
- [bootstrap] Single-tenant MVP; multi-tenant architecture is an explicit Non-Goal — §2.2 Non-Goals.
- [bootstrap] No advanced BI analytics dashboards in MVP — §2.2 Non-Goals.
- [bootstrap] No complex enterprise RBAC beyond practical admin/operator needs — §2.2 Non-Goals.
- [bootstrap] Telegram-only; cross-channel support is out of scope — §2.2 Non-Goals.
- [bootstrap] Option B implementation strategy is confirmed and scoping; deviations need re-confirmation — §1.
- [bootstrap] FastAPI-centered architecture is the chosen application framework — §1.
- [bootstrap] Docker-first deployment with local/dev/prod parity is mandatory — §1, FR-12, §9.
- [bootstrap] PostgreSQL is the system of record for transcripts and core tables; Qdrant is the vector store — §1, FR-5, §6.
- [bootstrap] Russian-first text handling is the operating assumption for the platform — codebase/CLAUDE.md context aligned with grounded-RAG scope (§FR-2).
- [bootstrap] HITL fallback is the core safety philosophy: uncertain answers escalate to a human, never ship — FR-3, FR-13, §8.
- [bootstrap] AI answers must be grounded in retrieved content; ungrounded/low-confidence output is blocked — FR-2, FR-13.
- [bootstrap] Answer guardrail decision engine gates every answer on retrieval sufficiency, grounding, confidence, and safety before delivery — FR-13.
- [bootstrap] Full conversation transcripts are persisted intact regardless of knowledge extraction — FR-5.
- [bootstrap] Knowledge enters the index only via a mandatory moderation gate (approve/reject) — FR-5, FR-6, §8.
- [bootstrap] Knowledge corrections create new versions; historical answer traces are append-only and never rewritten — FR-15, FR-17.
- [bootstrap] HITL recipient/config is admin-gated to a configured username (HITL_CONFIG_ADMIN_USERNAME, @ajdevy); non-admin attempts are rejected and audit-logged — FR-4.
- [bootstrap] Operator identity and Telegram forward metadata must never leak to end users — FR-3.
- [bootstrap] Secrets are environment-managed and never committed; all admin actions are auditable — NFR-3.
- [bootstrap] Critical operational incidents alert @ajdevy on Telegram, deduped/throttled by policy window — FR-8, §8.
- [bootstrap] All services expose /health/live, /ready, /startup with dependency-aware readiness — FR-9.
- [bootstrap] Structured JSON logging with trace_id/conversation_id correlation across services is required — FR-10, NFR-2.
- [bootstrap] External-provider resilience (retry/backoff/jitter, rate-limit handling, circuit breaker) degrades to HITL — FR-11, §8.
- [bootstrap] Qdrant supports routine backup and controlled restore with defined RPO/RTO; surfaced in Web UI — FR-14, NFR-7.
- [bootstrap] DigitalOcean is the documented, reproducible deployment target — §1, NFR-6.
- [bootstrap] Post-MVP tenant capabilities (FR-15–17) live in Epic 08 and assume Epics 02/03/05/06 complete — §10.

## Decisions (this session) — Calendar Availability & Scheduling (Epic 11), 2026-05-22

- Added Feature Group "Calendar Availability & Scheduling" with FR-18 (Operator OAuth connect), FR-19 (availability answering), FR-20 (per-service rules), FR-21 (per-project opt-in gating). — §4.
- **Read-only first**: availability checking only; calendar write/booking (event creation) is deferred and added as an explicit current Non-Goal. — §2.2.
- **Availability = Google Calendar `freeBusy` ∩ per-service rules** (duration, working hours, service-days). Free on calendar but outside rules = not available. — FR-19/FR-20.
- **Operators connect their OWN calendar via Telegram** 3-legged OAuth (consent URL → callback validates `state`+identity → encrypted per-operator refresh token); read-only scope; disconnect supported. — FR-18.
- **Token storage decision (user-confirmed):** per-operator refresh tokens encrypted at rest in dedicated SQLite (Fernet); OAuth client secret + encryption key in env; never logged/in env/in answer-trace. Extends NFR-3. — chosen over external secret-manager.
- **Opt-in per project, default-off**; tri-state pipeline behavior (not-enabled → silent no-op; enabled-but-disconnected → helpful/escalate; connected → answer). — FR-21.
- **Escalate-on-uncertainty**: a wrong "free" is worse than an escalation; counter-metric added. — FR-19, §7, §8.
- **Reconciliation of stale Non-Goals (user-approved "Approve all"):** "Multi-tenant architecture" non-goal marked superseded by shipped Epics 08/10 scoping; "cross-channel" clarified as customer-channel-only (GCal is an integration, not a channel); per-operator credential ownership clarified as not-RBAC. — §2.2.
- New data stores: `calendar_project_settings`, `calendar_operator_tokens` (encrypted), `calendar_service_rules`. — §6.
- Mapped to **Epic 11** in Delivery Mapping; depends on Epics 01/03 (pipeline), 08/10 (scoping), 02 (incidents), 09 (operator commands). — §10.

### Validation resolutions (rubric + adversarial review, 2026-05-22)

- **C1 (whose calendar):** v1 = **one designated calendar operator per project**; multi-operator selection deferred. — FR-18/19/20/21, §6 `calendar_project_settings`.
- **C2 (which service):** new **FR-22 Service Resolution** — lemma-match via `RussianNormalizer`; no/ambiguous/no-service-named → one clarifying turn → escalate; never guess.
- **C3 (whose timezone):** all customer + rule times interpreted in the **project timezone**; tz stored in `calendar_project_settings`. — FR-19/FR-20, §8.
- **C4 (Google-side revocation):** detect on next use → reconnect state + Telegram notify + **incident emitted** + dead token cleared. — FR-18, §8.
- **H1 (OAuth app verification):** sensitive-scope verification added as a **release-readiness gate** (§9) + risk (§8); pilot operates within test-user limits.
- **H2 (`state`):** single-use, server-stored, ~5-min TTL (mirrors login-code); new `calendar_oauth_pending_state` store. — FR-18, §6.
- **H3 (refresh-token long-term expiry):** 7-day-Testing / 6-month-unused / token-cap handled via detect→reconnect→notify→incident. — FR-18, §8.
- **H4 (token collisions):** `calendar_operator_tokens` **upsert on (project, operator)**; re-consent overwrites. — FR-18, §6.
- **H5 (latency guarantee):** softened — "config check precedes intent/API work" is the binding requirement; exact pipeline placement decided in architecture. — FR-21.
- **H6 (callback exposure/identity):** `state` is the sole browser↔operator binding (browser isn't Telegram-authenticated); callback rate-limited, renders success/failure page. — FR-18.
- **Medium/Low:** illustrative-strings-are-Russian-data note (FR-19); **no freeBusy result caching** in v1 (one live call/question, FR-19); working-hours **windows + service-days + date exceptions/RU holidays** (FR-20); **`[start, start+duration)`** slot-fitting + 60-day look-ahead default (FR-19); escalation routed to the **calendar operator** with context (FR-19); calendar incident types via Epic-02 integration (FR-18, §8); **Glossary** added (§11, resolves "service" overload); disconnect = best-effort Google revoke then local delete (FR-18); "bookable"→"schedulable" (FR-20); **primary calendar only** in v1, multi-calendar deferred (FR-19 v1 note); metric target framing (§7).
- **Deferred (explicit):** multi-operator selection, multi-calendar selection, booking/event-creation (read-only first).

### Architecture decisions (Epic 11, 2026-05-22) — appended to architecture.md

- **Pipeline placement:** standalone `CalendarAvailabilityAnswerer` placed **before** `GroundedRagAnswerer` (deterministic answer, must short-circuit RAG; cheap opt-in gate = fast `handled=False`). Reuses existing scheduling-intent regex + `RussianNormalizer`. — chosen over a `scheduling_context` signal.
- **OAuth callback home:** **`api`** (co-located with token store/client; avoids cross-service handoff of the auth `code`); minimal HTML page; rate-limited; `state` validated against SQLite pending-state store. `web_ui` stays the admin UI.
- **Auth vs transport:** `google-auth-oauthlib` Flow (code exchange) + `google-auth` `Credentials.refresh()`, both **sync via `asyncio.to_thread`** (google-auth owns token transport); **`freeBusy` over injected httpx `AsyncClient` with explicit timeout**. Reject `google-api-python-client`.
- **New stores:** `.data/semantaix_calendar.db` (one-file-per-concern) → `calendar_project_settings`, `calendar_operator_tokens` (Fernet), `calendar_oauth_pending_state` (single-use TTL), `calendar_service_rules`.
- **Availability core:** pure clock-injected tz-aware `compute_availability(...)`; slot-fitting `[start, start+duration)`; sync repos via `to_thread`.
- **Service resolution (FR-22):** `calendar/service_resolver.py` using `RussianNormalizer.lemmas`.
- **Resilience (user-requested):** per-operator `asyncio.Lock` single-flight refresh; refresh-token expiry/revocation → reconnect + notify + incident + clear token; explicit httpx + google-auth timeouts → escalate; inbound rate-limit on callback + `/connect_calendar`; outbound 429 `Retry-After` + one bounded retry; one freeBusy call/question, no result caching (v1).

### Epics & Stories (Epic 11, 2026-05-22) — created in house per-epic-file convention

- Followed the existing `epics/epic-NN-*.md` + `stories/epic-NN/` layout (epic-10 shape), **not** the skill's generic `epics.md` template.
- Epic file `epic-11-calendar-availability-scheduling.md` (Goal / In-Out scope / Dependencies / Exit Criteria / E2E). README Epic Order + notes updated.
- **7 stories (one PR each):** 11.01 schema+settings+repos (foundation) · 11.02 OAuth connect (api) · 11.03 Telegram connect/disconnect command · 11.04 token lifecycle + freeBusy client + resilience · 11.05 pure availability engine · 11.06 service resolution (FR-22) · 11.07 answerer + pipeline wiring. Dependency graph in `stories/epic-11/README.md`.
- Coverage: each story carries unit/contract/E2E with 100% gate; E2E under `tests/e2e/test_e2e_epic11_*.py`; `scripts/epic11_signoff.sh` + `e2e-coverage.md` rows.

### Implementation readiness (Epic 11, 2026-05-23) — report at implementation-readiness-report-2026-05-22.md

- Verdict: **READY** after resolving 2 findings. Full FR-18–FR-22 → story traceability; sound dependency order; deferred items consistent across PRD/architecture/epic/log.
- **Finding 1 (config entry point) → resolved:** added **story 11.08** (enable/disable + service-config surface). **Permission model (user decision):** operator **and** admin can enable/disable a project's calendar; **disable keeps the token**; **only the operator can disconnect/delete** the integration (admin → 403). PRD FR-18 (disconnect operator-only) and FR-21 (enable/disable permissions, disable≠delete) updated.
- **Finding 2 (clarify-once) → resolved:** story 11.07 now requires exactly **one clarifying turn** (FR-22 unchanged); hedge removed.
- Epic 11 is now an **8-story** pack (11.01–11.08).

## Decisions (this session) — Calendar `/connect_calendar` auto-enables (Epic 11 follow-up), 2026-05-24

- **Drop the separate enable surface.** User directive: "we don't need a separate /calendar_on command, just make it on when using /connect_calendar." The operator `/calendar_on` command, the admin `/calendar_on @projectslug` command, and the `POST /calendar/projects/{id}/enable` endpoint are **removed**. `ApiClient.calendar_enable` is gone too.
- **Enable is implicit in `/connect_calendar`.** The `GET /calendar/oauth/callback` handler now calls `CalendarSettingsRepository.enable(...)` after the token upsert, atomically (best-effort): on success it flips `enabled=1` and records the connecting operator as the designated calendar operator; if `enable` raises, the callback returns a 500-class error and does NOT render the success page (the operator retries by re-running `/connect_calendar`). For an already-enabled project, the existing `project_timezone` / `lookahead_days` are preserved — only the designated operator may change. — FR-18, FR-21, story 11.08.
- **Disable + service config remain unchanged.** `/calendar_off` (operator + admin), `POST /disable`, `GET /settings`, and the service-rule routes (`POST .../services`, `DELETE .../services/{id}`) are intact. `/calendar_service add|remove` is unchanged. Re-enable after disable = the operator re-runs `/connect_calendar` (re-runs Google consent, replaces the token, re-flips `enabled=1`).
- **Disconnect is still operator-only.** `/disconnect_calendar` deletes the token; an admin attempting `POST /calendar/disconnect` with `actor_role=admin` still gets 403 (`admin_cannot_disconnect`).
- **Rationale.** Eliminates a redundant config step: the operator is already in front of Google consent, so requiring a second `/calendar_on` was friction without a permission benefit (an admin still cannot enable without an operator's consent — by design, since enable now requires real OAuth tokens). Disable stays explicit + shared (operator + admin) so an admin can pause an integration without the operator's involvement.
- **Affected docs:** PRD FR-18 + FR-21; architecture.md (Connect flow); epic-11-calendar-availability-scheduling.md (In Scope bullet); story 11.08 (rewritten); project-context.md (calendar tri-state rule tightened); README dependency note; `scripts/epic11_signoff.sh` (uses `CalendarSettingsRepository.enable` directly, since a full Google round-trip is offline-only).
- **Affected code:** `services/api/app/main.py` (callback auto-enables; `/enable` endpoint and `CalendarEnableRequest` removed); `services/bot_gateway/app/calendar_commands.py` (`/calendar_on` operator command removed); `services/bot_gateway/app/admin_commands.py` (`/calendar_on @slug` removed; `handle_calendar_toggle` renamed to `handle_calendar_off`); `services/bot_gateway/app/api_client.py` (`calendar_enable` removed).
- **Affected tests:** removed all `/calendar_on` + `POST /enable` tests across `test_api_calendar_config_contract.py`, `test_bot_gateway_calendar_config_commands.py`, `test_bot_gateway_api_client.py`, `tests/e2e/test_e2e_epic11_enable_and_configure.py`; added auto-enable + already-enabled-preservation + enable-after-token-upsert-failure tests in `test_api_calendar_oauth_contract.py`; rewrote the e2e to drive enablement through the OAuth callback. 100% coverage held.

### Epic 13 — Unified Project Services Catalog (planning kickoff, 2026-05-24)

**Problem being resolved.** Today the bot has two disjoint "services" concepts and the same offering ("маникюр") must be described twice:
- **Catalog answer** (PR `4d85819`): "what services do you offer?" is answered from an LLM-built prose digest of operator-uploaded PDFs (`catalog_digests` / `catalog_digest.py` / `service_catalog_intent.py`). No structured services table.
- **Calendar service rules** (Epic 11): `calendar_service_rules` in `semantaix_calendar.db` holds operator-curated structured rows for `compute_availability` and the calendar `service_resolver`.

The two never reference each other. The user wants ONE canonical structured services list per project that powers BOTH the catalog answer AND the calendar.

**Decisions locked.**
- **Rename `calendar_service_rules` → `project_services`** in the same SQLite DB. Idempotent ALTER (table is empty in production — zero data risk). Columns: `id, project_id, name (REQUIRED), description, price_text, tags_json, duration_minutes, working_hours_json, service_days_json, date_exceptions_json, updated_at`. — FR-23, §6.
- **Calendar-eligibility = `duration_minutes IS NOT NULL`.** Calendar code filters on that; catalog-only rows are valid (name + maybe price/desc, no duration). Calendar-only rows and dual-use rows are also valid. — FR-23.
- **Canonical CRUD seam = `ProjectServiceRepository`** (sync `sqlite3` via `asyncio.to_thread`). `CalendarSettingsRepository`'s service-rule method names remain for one release as delegating aliases to ease the cutover. — FR-23.
- **Two editing paths converge on one upsert.** `/service add|remove|list` and a Russian NL dialog (`добавь услугу …`) both apply via `ProjectServiceRepository.upsert`. Same DB state regardless of input path. `/calendar_service` remains for one release as a deprecation-logged alias. — FR-24.
- **NL dialog mirrors the existing `nl_knowledge_ops` / `admin_nl_dialog` pattern.** New `ServicesNlOpsRepository` + `services_nl_op_sessions` table, state machine `pending_confirmation → confirmed | cancelled | expired`, TTL 600s, `confirm_token = secrets.token_urlsafe(16)`, atomic consume via `hmac.compare_digest`. New api endpoints behind `internal_service_token`. New bot module `services_nl_dialog.py`. — FR-24, §6.
- **Operator-AND-admin can edit services** on projects where they are registered as an operator (Epic 10 `operators` table). Non-registered senders are ignored silently with logged reason `unauthorized_services` — no session row, no token, no DM. — FR-24.
- **Admin gate narrowing (cross-reference Epic 11):** FR-24's admin must ALSO be a registered project operator — this is **narrower** than FR-21's plain admin gate for `/calendar_off`. Rationale: services are project-content the operator curates, not platform-level config; only operators-of-this-project should curate them. Admin disconnect remains as in FR-18 (operator-only).
- **Regex-based extraction in v1 (no LLM in the NL extraction path).** Ambiguous Russian input fails closed ("не понял, уточните"). LLM-based extraction is a future epic. — FR-24.
- **Catalog answer reads structured services first.** When `project_services` is non-empty for `ctx.project_id`, render as a labelled all-fields plain-text chunk and pass to the existing `answer_grounded` step (no extra LLM call vs today's digest). When empty, fall back to `_catalog_digest.get_digest(...)`. When both empty, existing `_skip(reason='catalog_empty')`. — FR-25.
- **Humanistic, question-tailored rendering.** Extend `grounding_system` with a Russian guidance rule so a "какие услуги?" question gets just the names and a "сколько стоит маникюр?" question gets just that service's price — no full dumps. — FR-25.
- **Audit logs keys, not values.** `services_nl_op_confirmed` records `trace_id, project_id, operator, op_type, payload_keys` — never the price/description content (they may carry sensitive data). — FR-24.
- **Delivery shape:** one combined PR for the Epic 13 code, but story-by-story inside (matching prior epics).

**Conflicts surfaced (and resolved in the 2026-05-24 validation pass — see "Validation resolutions" sub-section below).**

The original kickoff entry above claimed "(none)" — that claim was overconfident. The rubric and adversarial reviews caught three near-conflicts that the original framing walked past, all resolved during the validation pass:

1. **Permission-model asymmetry with Epic 11 (C1).** FR-21 splits permissions sharply: **disable** (reversible) is operator-AND-admin, but **disconnect** (destructive — deletes the operator-owned token) is operator-only. The original FR-24 kickoff declared "operator AND admin can add/edit/remove services" — collapsing that split and letting an admin perform destructive writes on operator-curated content. **Resolved:** `/service add` and `/service edit` are operator-AND-admin (non-destructive, analogous to enable/disable); `/service remove` is operator-only (destructive, analogous to disconnect) — admin attempting `/service remove` returns 403 `admin_cannot_remove_service`. Admin must additionally be a registered project operator (narrower than FR-21 — see admin-gate-narrowing bullet).
2. **Audit "keys not values" cargo-culted from FR-18's secret-handling rule (H5).** The original FR-24 said `services_nl_op_confirmed` logs only `payload_keys`, never values — borrowed wording from FR-18 / NFR-3's OAuth-token redaction rule. But `price_text` and `description` are non-secret operator-published content (the bot reads them back to every customer), and a keys-only audit makes the dispute path "who set маникюр's price to 9999 ₽ on 2026-05-30?" unanswerable. **Resolved:** log the full payload (same posture as today's `answer_traces`). NFR-3's OAuth-redaction scope is unchanged.
3. **"One release" deprecation window contradicted by the immediately-prior Epic 11 follow-up (H2).** FR-23 / FR-24 kept aliases "for one release" but the same 2026-05-24 decision-log entry (Epic 11 follow-up) **deleted** `/calendar_on` with no deprecation period — establishing a "one release = zero" precedent in this codebase. **Resolved:** aliases (`CalendarSettingsRepository.upsert_service_rule`, `/calendar_service`, `POST/DELETE /calendar/projects/{id}/services`) are pinned to the **Epic 14 cleanup PR (no later than 60 days after Epic 13 merge)**, and deprecated paths DM the operator a user-facing migration hint, not just log.

NFR-3's OAuth/secrets posture is preserved: `project_services` rows carry no per-operator credentials, so the encryption-at-rest mandate does not apply to this table. The only original framing that stands unchanged is "no prior decision asserts that `calendar_service_rules` must remain private to the calendar feature" — the rename / unification itself is consistent with the existing "config in DB, repository-owned" pattern.

**Explicitly deferred.**
- LLM-based extraction of services from `/kb_add`-uploaded PDFs (operator uploads a service-list PDF; the system parses it into `project_services` rows) — future epic.
- Web admin UI for `project_services` CRUD — bot-only in Epic 13.
- Multi-operator selection / multi-calendar selection (still Epic 11 deferrals; unchanged here).
- Booking / event-creation (still the §2.2 Non-Goal; unchanged).

### Validation resolutions (rubric + adversarial review, 2026-05-24)

22 findings — 4 critical, 6 high, 7 medium, 5 low — all resolved in this pass. Reports: `validation/validation-report-2026-05-24.md` + `.html`; reviewer files `validation/review-rubric-epic13.md`, `validation/review-adversarial-epic13.md`.

**Critical (4):**
- **C1 (permission split for `/service remove`):** Operator-AND-admin for `add`/`edit`; **`remove` is operator-only** (admin → 403 `admin_cannot_remove_service`). Matches Epic 11's destructive=operator-only rule (FR-18/FR-21). — FR-24.
- **C2 (uniqueness + upsert key):** `UNIQUE(project_id, lower(name))`; `ProjectServiceRepository.upsert` keyed on `(project_id, lower(name))`; duplicate-name attempts update existing row and emit `services_upsert_duplicate_name`; FR-22 lemma resolution runs against project-scoped calendar-eligible subset, with "ambiguous → one clarifying turn → escalate" inherited verbatim. — FR-23, FR-24, FR-22.
- **C3 (binary cliff regression):** FR-25 now **merges** structured rows AND digest content with **deduplication** (lemma-match of structured row name against digest tokens; structured row wins on conflict). When in doubt, both are kept. Single-row insert no longer silently shrinks a 12-service digest to one row. — FR-25.
- **C4 (NL threat model):** Preview DM rendered as plain text (no MarkdownV2/HTML), operator content escaped + 200-char cap; confirm endpoint verifies `session.originating_operator == current_sender` (cross-operator replay → 403 `not_session_owner`); at most ONE pending session per `(project_id, operator)` — second trigger cancels prior and DMs "ваш предыдущий запрос отменён". — FR-24.

**High (6):**
- **H1 (migration idempotency):** Genuinely idempotent via existence-check guards (sqlite_master + PRAGMA table_info), not blind ALTER. Fresh-deploy path CREATEs `project_services` directly when neither old nor new table exists. Acceptance: "running twice is a no-op" + "fresh deploy without Epic 11 migrations succeeds". — FR-23.
- **H2 (deprecation window pinned):** Aliases removed in **Epic 14 cleanup PR, no later than 60 days post-Epic-13-merge**; deprecated paths log AND DM a user-facing migration hint. — FR-23, FR-24.
- **H3 (concurrency):** Per-`(project_id, lower(name))` `asyncio.Lock` around `ProjectServiceRepository.upsert` (single-flight, mirroring Epic 11's token-refresh lock). Last-writer-wins is the v1 semantics; no optimistic concurrency / `updated_at` checks. — FR-23.
- **H4 (label leak + verifier collision):** Rendering moved to repository boundary as **natural Russian prose** ("Маникюр — 60 минут, пн–сб 10:00–19:00, цена от 2000 ₽."), no field-label tokens. Acceptance: customer-visible answer contains no `Название:` / `Цена:` substrings. `data/russian_hedges.txt` audited before merge against price/duration phrasings. — FR-25.
- **H5 (audit logs full values):** `services_nl_op_confirmed` / `*_cancelled` / `*_expired` carry the FULL payload (name, description, price_text, tags, scheduling fields). Operator-published content is non-secret. Same posture as `answer_traces`. NFR-3's OAuth-redaction scope unchanged. — FR-24.
- **H6 (JSON shapes + Russian rendering map):** `working_hours_json` `{"mon":[["10:00","19:00"]]}`, `service_days_json` `["mon",…]`, `date_exceptions_json` `["2026-01-01",…]`. New data file `data/russian_calendar_terms.json` for Russian rendering; multi-window-per-day → `"пн 10:00–13:00, 14:00–19:00"`; date exceptions → `"закрыто: 1 января, 9 мая"`. — FR-23, FR-25.

**Medium (7):**
- **M1 (NL regex coverage):** Explicit "must parse" examples (Cyrillic dash variants, "ё"/"е" normalization, multi-token names) and "must fail closed" examples (two-in-one, non-digit duration) added to FR-24 acceptance. — FR-24.
- **M2 (no-data-dump bound):** FR-25 AC #2 replaced with concrete bound: "single-service question → at most one `price_text` and at most one `description`; general question → names only, no prices/descriptions unless explicitly asked". — FR-25.
- **M3 (source_id pinned):** `answer_traces.source_id` literals `project_services:<id>` / `catalog_digest:<id>` / `merged:<id>` pinned in §6 row for `semantaix_answer_traces.db`. — §6.
- **M4 (trigger collisions):** Triggers anchored to start-of-message (regex `^\s*(добавь|…)\s+услугу\b`); non-registered senders ignored with **no DM** (logged `unauthorized_services`) to avoid silent customer-thread weirdness. — FR-24.
- **M5 (payload audit):** Resolved by H5 (full payload logged). Additionally `services_nl_op_sessions` rows are soft-deleted on confirm (30-day retention), expired sessions reaped lazily. — FR-24.
- **M6 (migration scope):** Migration touches ONLY the rename and the four new columns; `calendar_project_settings` / `calendar_operator_tokens` / `calendar_oauth_pending_state` are unchanged (verified by schema snapshot). — FR-23.
- **M7 (FR-22 stale wording):** FR-22 now says "configured **schedulable** service" with explicit `duration_minutes IS NOT NULL` predicate; catalog-only rows are intentionally invisible to the calendar resolver. — FR-22.

**Low (5):**
- **L1 (§10 omission):** Epic 13 paragraph added to §10 (depends on Epic 11/10/09; references epic file to be created). — §10.
- **L2 (wrong DB):** `services_nl_op_sessions` moved from `semantaix_calendar.db` to `semantaix_nl_ops.db` (alongside `nl_op_sessions`, `admin_nl_op_sessions`). — §6.
- **L3 (overconfident "no conflicts"):** Rewritten as three explicit near-conflicts (C1, H5, H2) with how each is resolved. — this sub-section.
- **L4 (glossary eligibility split):** Glossary "Project service" entry now states "catalog-eligible always; calendar-eligible iff `duration_minutes IS NOT NULL`" with FR-23/FR-20/FR-22 cross-refs. — §11.
- **L5 (admin-gate narrowing not flagged):** Added bullet to the Epic 13 decision-log entry noting FR-24 admin must ALSO be a registered project operator (narrower than FR-21). — this entry.

**Verdict after resolution:** READY for architecture. All 22 findings resolved. Phase 4 (epic + stories regeneration, code) proceeds.

### Epic 13 architecture (2026-05-24) — appended to architecture.md

- Component map (8 components) + 4 request flows (slash edit, NL edit, catalog read, calendar read).
- Pipeline placement unchanged (GroundedRagAnswerer catalog branch extended; no new answerer needed for Epic 13).
- Authorization helpers: `authorize_calendar_config` reused for add/edit; new `authorize_service_remove` for operator-only remove (admin → 403 `admin_cannot_remove_service`).
- New SQLite tables: `project_services` (rename + new columns in `semantaix_calendar.db`); `services_nl_op_sessions` (in `semantaix_nl_ops.db`).
- New data file: `data/russian_calendar_terms.json` (day-code + month-name maps for prose renderer).
- Concurrency model: per-`(project_id, lower(name))` `asyncio.Lock` single-flight upsert; last-writer-wins.
- Deprecation: 60-day window through Epic 14 cleanup PR; deprecated paths log AND DM migration hint.

### Epic 13 stories pack created (2026-05-24)

- Epic file + stories README + 6 story files written under planning-artifacts/epics{,/stories/epic-13}/.
- Story 13.01 owns the data file `data/russian_calendar_terms.json` (created alongside the schema).
- One combined PR shape preserved: 13.01 → {13.02 → 13.03 ∥ 13.04 → 13.05} ∥ 13.06. Each story carries its own 100% coverage gate; merged together.
- epics/README.md Epic Order updated; Recent Implementation Notes paragraph appended.

### Refinement R2 (post-13.04, 2026-05-24): connect-confirmation Telegram DM

- Closes a missed FR-18 acceptance criterion ("Telegram confirmation" on consent success).
- Reuses `telegram_bot_sender` + `operator_repository.find_by_username`; falls back to `settings.hitl_primary_operator_chat_id`; failures logged + swallowed (do not affect OAuth handshake or HTML success page).
- DM copy includes a "next step" hint nudging operators toward `/service add` (catalog-only entries).
- Three new branch tests (`tests/test_api_calendar_oauth_contract.py`) + e2e assertion (`tests/e2e/test_e2e_epic11_oauth_connect.py`).
- PRD FR-18 sub-bullet added.

### Epic 16 planning (2026-06-16) — Operator self-registration & onboarding

- **Self-service entry:** `/register [display_name]` in bot for non-operators; creates `operator_registration_requests` row (`pending`).
- **Admin gate:** Approve/Reject via Telegram inline buttons (`callback_query` — new bot_gateway capability, currently ignored). Callback payload convention: `op_reg:approve:<id>` / `op_reg:reject:<id>` (≤64 bytes).
- **Default project:** v1 assigns `default` project on approve; project picker on approval deferred.
- **Rejection cooldown:** 24 h server-enforced per username before re-register.
- **Post-approval onboarding:** DM with inline buttons `onboard:cal:<operator_id>` (Epic 11 `/connect_calendar` reuse) and `onboard:tg:<operator_id>` (Epic 15 QR flow extended per-operator).
- **Per-operator Telegram sessions:** `.data/operator_sessions/{operator_id}.session` + `operator_telegram_auth` table; `user_gateway` auth endpoints accept `operator_id`.
- **Platform bot identity (2026-06-16):** Renamed to **Semantaix** (`@semantaix_bot`). Settings: `BOT_PERSONA_FIRST_NAME=Semantaix`, `TELEGRAM_PLATFORM_BOT_USERNAME=@semantaix_bot`. Sales LLM persona decoupled to `SALES_PERSONA_*` (default Анна Иванова). FR-16-16.
- **Epic sequencing:** Epic 16 starts after Epic 15 stories 15.01–15.02 (QR auth substrate). Story 16-06 extends 15.02, does not duplicate QR logic.
- **Artifacts:** product brief, PRD addendum (FR-16-01..12, NFR-16-01..06), epic file + 7 stories under `epics/stories/epic-16/`.

### Refinement R1 (post-13.04, 2026-05-24): name-only service add

- Legacy `parse_service_add` relaxed from `len(parts) == 5` → `len(parts) >= 2`; missing fields default to None.
- `/service` slash success DM appends a Russian "add scheduling later" hint when the saved row is catalog-only (`duration_minutes is None`); omitted when scheduling fields are present.
- Explicit name-only tests added to 4 test files; one previously-rejecting parametric case removed.
- PRD FR-24 + stories 13.03/13.04 In-Scope sentences added.
